version: '3'

services:
  sockets-server:
    build:
      context: ./php-fpm
      dockerfile: Dockerfile
    image: sockets/server
    container_name: sockets-server
    volumes:
      - ./project:/data
      - ./socket:/tmp

