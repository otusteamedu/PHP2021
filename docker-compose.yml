version: '3'

services:
  server:
    container_name: server-php-fpm
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/var/www
      - /tmp:/tmp
  client:
    container_name: client-php-fpm
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/var/www
      - /tmp:/tmp