version: '3'

services:
  app:
    build:
      context: ./fpm
      dockerfile: Dockerfile
    image: otusapp/php
    container_name: app
    volumes:
      - ./app:/var/www/app
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
