<?php

namespace Elastic\ElasticSearch;

use Elastic\Youtube\Api;
use Elastic\Models\Channels;
use Elastic\ElasticSearch\Elastic;

class Channels
{ 
    $modelsChannels = new Channels;
    $elastic = new Elastic;
    $client = $date->createIndex($this->);
}